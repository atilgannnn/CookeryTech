package com.cookerytech.repository;

import com.cookerytech.domain.ModelPropertyKey;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModelPropertyKeyRepository extends JpaRepository<ModelPropertyKey, Long> {
}
