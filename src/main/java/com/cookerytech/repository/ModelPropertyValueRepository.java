package com.cookerytech.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ModelPropertyValueRepository extends JpaRepository<ModelPropertyValueRepository, Long> {
}
